<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_aulago"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_aulago_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AulaGo">
        <activity
            android:name=".SolicitarSerProfessorActivity"
            android:exported="false"
            android:label="Solicitar Ser Professor" />

        <activity
            android:name=".EditarPerfilAlunoActivity"
            android:exported="false"
            android:label="Editar Perfil de Aluno" />

        <activity
            android:name=".CadastroActivity2"
            android:exported="false" />

        <activity
            android:name=".CadastroActivity"
            android:exported="false" />

        <activity
            android:name=".CalendarActivity"
            android:exported="true"
            android:label="Calendário"/>

        <activity
            android:name=".EditarPerfilProfessorActivity"
            android:exported="true"
            android:label="Editar Perfil de Professor" />

        <activity android:name=".AlunoPerfilActivity"
            android:exported="true"
            android:label="Perfil do Aluno" />

        <activity
            android:name=".EditarDadosPessoaisActivity"
            android:exported="false"
            android:label="Editar Dados Pessoais" />

        <activity
            android:name=".ProfessorPerfilActivity"
            android:exported="true"
            android:label="Perfil do Professor" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>